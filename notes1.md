# What is Markdown?
* Markdown is another way to write plain text documents like microsoft word, or google docs but with a few lightweight formatting options. Its future proof and does not require a HTML, instead allows to convert the plain text documents. 
  
# What is Git?
* Is a version control system that runs on the machine. 

# What is GitHub?
* Hosted in the cloud/web to host git 

# What is Slack?
* Slack is another form of instant messaging to keep in contact with the class and the professor. 